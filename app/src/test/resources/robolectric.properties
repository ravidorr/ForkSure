# Robolectric Configuration
# Use Android API 34 instead of 35 for Robolectric compatibility
sdk=34

# Enable newer Android features
# Robolectric doesn't support API 35 yet, so we use 34 for tests
manifest=--default
qualifiers=large-port-xhdpi